query Constants($language: LanguageEnum = ENGLISH) {
  constants {
    gameVersions {
      id
    }
    facets(language: $language) {
      name
      id
      color
      icon
      language {
        displayName
      }
    }
  }
}
